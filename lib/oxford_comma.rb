def oxford_comma(array)
  case array
    when array.size == [1]
      return "#{array[0]}"
    when array.size == [2]
      return array.join(" and ")
    when array.size >= [3]
      new_array << 
      return new_array
  end
end


## CONVERT ARRAY TO STRING
